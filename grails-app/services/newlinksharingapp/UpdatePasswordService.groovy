package newlinksharingapp

import grails.transaction.Transactional

@Transactional
class UpdatePasswordService {

    def savePassword(String email) {
//       println email
//       String sEmail = email
//       String newPassword = params.newpassword

/*
        write the logic to save the new password into database where email = sEmail

        def u1 = Users.findByEmail(sEmail)
        u1.password = newPassword
*/



    }
}
